/** Hard ceiling for the PDF compressor. Files above this are rejected up front. */
export const MAX_COMPRESS_BYTES = 50 * 1024 * 1024;

export const PRIVACY_LINE =
  'Files are processed on your device and never uploaded.';
